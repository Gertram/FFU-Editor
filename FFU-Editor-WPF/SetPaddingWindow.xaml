<mah:MetroWindow x:Class="FFUEditor.SetPaddingWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:mah="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
    xmlns:local="clr-namespace:FFUEditor"
    mc:Ignorable="d"
    SizeToContent="WidthAndHeight"
    Title="{DynamicResource m_SetPaddingWindowW_Title}">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="150"/>
            <ColumnDefinition Width="150"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Label Grid.Row="0" Grid.Column="0" Content="{DynamicResource m_SetPaddingWindow_Label}" Margin="10,5"/>
        <TextBox 
            Grid.Row="0" Grid.Column="1" Margin="10,5"
            x:Name="ValueTextBox"/>
        <Button 
            Grid.Row="1" Grid.Column="1" Margin="10,5"
            x:Name="OkButton"
            Click="OkButton_Click"
            Content="{DynamicResource m_Ok_Header}"/>
    </Grid>
</mah:MetroWindow>
